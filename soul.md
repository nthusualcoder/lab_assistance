# 어플리케이션 제작 핵심 수칙 (soul.md)

본 문서는 실험 보조 계산기 앱을 설계, 구현, 확장할 때 반드시 준수해야 하는 핵심 디자인 철학, 아키텍처 규칙 및 연산 수식을 규정합니다. 모든 개발 작업은 이 문서에 기술된 원칙을 따릅니다.

---

## 1. UI & UX 디자인 수칙 (Toss Style)

토스(Toss) 앱의 세련되고 직관적인 인터페이스를 재현하기 위해 다음 시각적 명세를 엄격히 준수합니다.

### 1.1. 컬러 팔레트 (Color Palette)
- **배경색 (Background)**: `#f2f4f6` (앱 전체 화면의 배경, 깊이감 부여)
- **카드/컨테이너 배경**: `#ffffff` (콘텐츠 영역은 백색 카드로 띄움)
- **브랜드 블루 (Toss Blue)**: `#3182f6` (기본 확인 버튼, 탭 활성 상태, 하이라이트)
- **보조 블루 (Light Blue)**: `#e8f3ff` (비활성 선택 상태, 프리셋 활성화 백그라운드)
- **주요 텍스트 (Primary Text)**: `#191f28` (제목, 강조 텍스트, 검은색에 가까운 무거운 색)
- **보조 텍스트 (Secondary Text)**: `#4e5968` (설명글, 레이블, 회색조)
- **설명/비활성 텍스트 (Muted Text)**: `#8b95a1` (단위 표시, 플레이스홀더)
- **경고/에러 컬러 (Error Red)**: `#f04438` (입력값 오류 및 경고 메시지)
- **테두리선 (Divider/Border)**: `#e5e8eb` (구분선 및 얇은 테두리)

### 1.2. 기본 테마 설정 (Default Theme)
- 사용자의 시력 보호를 위해 **다크 모드(Dark Theme)가 기본적으로 작동**되도록 선언합니다. 다크 모드에 맞는 명도 대비 가독성을 엄수합니다.

### 1.3. 타이포그래피 & 여백 (Layout & Typography)
- **글꼴**: `Inter`, `-apple-system`, `sans-serif` 적용.
- **둥근 모서리 (Border-radius)**: 
  - 카드 컨테이너: `20px` ~ `24px`
  - 버튼 및 입력 필드: `14px` ~ `18px`
- **입력 필드 스타일**: 깔끔하게 위쪽에 레이블을 고정시키고, 테두리가 둥글고 넓은 라운드 박스로 배치합니다.

---

## 2. 코드 아키텍처 및 확장성 수칙 (Modularity)

### 2.1. 단일 페이지 앱(SPA) 라우팅 설계
- 모든 화면은 `index.html` 내에서 별도의 섹션(`<section id="page-...">`)으로 선언되며, `app.js` 내의 `switchPage(pageId)` 라우팅 메커니즘을 통해 페이지 간 매끄럽게 페이드 아웃/인 애니메이션을 거쳐 전환됩니다.

---

## 3. 기능성 및 물리 수식 명세

### 3.1. Collagen 제조 용량 계산기
- **기본 고정 비율**: `Collagen : 10xPBS : 1M NaOH = 40 : 4 : 1` (부피 비율)
  - $V_{PBS} = V_{col} \times 0.1$
  - $V_{NaOH} = V_{col} \times 0.025$
- **시약 정렬 순서**: 반드시 `콜라겐 -> 10X PBS -> SFM -> 1M NaOH` 순서로 일괄 정렬 및 표기합니다.
- **SFM 및 총 부피 계산 공식**:
  - $V_{total} = V_{col} \times (C_{init} / C_{target})$
  - $V_{SFM} = V_{total} - 1.125 \times V_{col} = V_{col} \times (C_{init} / C_{target} - 1.125)$
  - SFM 막대 게이지 색상은 **Toss 핑크색(#ff6584)**으로 렌더링하고, 글씨 스타일은 기본 크기/굵기로 노출합니다.
- **농도 프리셋 종류**:
  - 프리셋 1: 시작 `8.64` -> 목표 `6` mg/mL
  - 프리셋 2: 시작 `3.75` -> 목표 `3` mg/mL (정밀 적용)
  - 프리셋 3: 직접 입력 (Custom)

### 3.2. Hemocytometer 세포 카운팅 계산기
세포 현탁액의 농도를 계산하고, 실험에 필요한 용액 조제법을 안내합니다.

1. **세포 농도 계산 공식**:
   사용자가 15칸 전체를 세어 카운팅한 세포 수 $N$ 일 때, 세포 농도($C_{cell}$)는:
   $$C_{cell} = N \times 2 \times 10^4 \text{ cells/mL}$$
   - **출력 형식**: 곱하기 부호 `*` 대신 **`×`**를 사용하고 지수 기호 `^` 대신 **HTML 위첨자 `<sup>`**를 사용하여 표현합니다. (예: `3.00 × 10<sup>5</sup> cells/mL`)
   - **전체 세포 수 계산**: 기본 1 mL 총 용량($V_{stock}$) 기준 전체 세포 수 계산 ($T_{cell} = C_{cell} \times V_{stock}$). $V_{stock}$은 사용자가 직접 편집 가능해야 합니다.

2. **지수 표기법 입력 UX**:
   - 지수를 입력하는 입력 필드는 문자 `^` 대신 실제 지수 위치(위첨자 위치)로 스타일링하여 시각적으로 $10^{Exponent}$ 형태로 구현합니다.

3. **하위 기능 2.1: 필요한 세포 수 수확 (Harvest)**:
   목표 세포 수 $T_{target}$ ($A \times 10^B$ cells)를 획득하기 위해 따야 할 원액 세포 현탁액 부피 $V_{harvest}$ ($\mu\text{L}$):
   $$V_{harvest} = \frac{T_{target}}{C_{cell}} \times 1000\ \mu\text{L}$$

4. **하위 기능 2.2: 특정 세포 농도/부피 제조 (Dilution)**:
   목표 세포 농도 $C_{target}$ ($X \times 10^Y$ cells/mL) 및 목표 제조 용량 $V_{target}$ (mL)을 만들기 위해 투입할 세포 용액 부피 $V_{cell}$ 및 배지(Media) 부피 $V_{media}$ ($\mu\text{L}$):
   $$V_{cell} = V_{target} \times \frac{C_{target}}{C_{cell}} \times 1000\ \mu\text{L}$$
   $$V_{media} = (V_{target} \times 1000) - V_{cell}\ \mu\text{L}$$
   - **기본 목표 농도**: 기본 목표 농도($C_{target}$) 설정은 **`5.0 × 10^5 cells/mL`**로 지정합니다.
   - **제약 조건**: 목표 농도가 현재 원액 세포 농도를 초과하는 경우($C_{target} > C_{cell}$), 희석 제조가 불가능하므로 경고 메시지를 노출합니다.

---

## 4. 검증 및 배포 수칙

1. **로컬 무선 테스트 환경**: `start_server.py`는 모바일 기기 접속 편의를 위해 터미널 창에 로컬 IP 주소로 인코딩된 접속 안내를 출력해야 합니다.
2. **PWA 오프라인 작동**: `sw.js`에 정의된 정적 캐싱 목록이 누락되지 않도록 빌드 전 확인하고, 네트워크가 끊긴 오프라인 비행기 모드에서도 실행되는지 검증합니다.
